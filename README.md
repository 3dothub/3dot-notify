## What is this?

A 3dot-notify 

##Installation

Run `npm i 3dothub`

Use:

```
import {Notify} from '3dothub';
const message = {
      id: 1,
      title: "Error",
      message: "An error occurred",
      type: "error"
    },
<Notify messages={message}>;

```

